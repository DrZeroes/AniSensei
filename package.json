{
  "name": "anisensei",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "generate:changelog": "node scripts/generate-changelog.mjs",
    "dev": "npm run generate:changelog && vite",
    "build": "npm run generate:changelog && vite build",
    "preview": "vite preview",
    "test": "npm run generate:changelog && vitest run"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.5.0",
    "@testing-library/react": "^16.0.1",
    "@testing-library/user-event": "^14.5.2",
    "@vitejs/plugin-react": "^4.3.1",
    "jsdom": "^25.0.0",
    "vite": "^5.4.6",
    "vitest": "^2.1.1"
  }
}
